{
  "buildCommand": "pip install -r requirements.txt && mkdocs build -d public/research && npm run build",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/docs/:path*",
      "destination": "https://platform.nwsldata.com/docs/:path*"
    }
  ]
}